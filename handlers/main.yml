---
- name: 'restart {{ app_name }}'
  systemd:
    name: '{{ app_name }}'
    state: restarted
    enabled: yes
    daemon_reload: yes

- name: 'restart nginx'
  systemd:
    name: 'nginx'
    state: restarted
    enabled: yes
    daemon_reload: yes
